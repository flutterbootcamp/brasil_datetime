# Exemplo

Este diretório contém um exemplo executável que demonstra todos os formatos
disponíveis no pacote.

Na raiz do projeto, execute:

```sh
dart run example/brasil_datetime_example.dart
```

Como o `brasil_datetime` é um pacote Dart sem interface gráfica, não é
necessário criar um aplicativo Flutter separado para experimentar a API.
